- We must modify the XML files:
  search/replace of
    invert="true"
  to
    invert="true" cascade="all"

- We must mofidy the JAVA files:
  in Patient:
    make getDatasets setDatasets package scope
  in Dataset:
    make getPatients setPatients package scope
    make setDatasetAccess package scope

