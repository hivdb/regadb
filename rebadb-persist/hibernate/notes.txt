- We must modify the XML files:
  search/replace of
    inverse="true"
  to
    inverse="false" cascade="all"

  but not:
     getDatasets in PatientImpl (?)

- We must mofidy the JAVA files:
  in PatientImpl:
    make class package scope

